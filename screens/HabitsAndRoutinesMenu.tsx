import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

interface HabitsAndRoutinesMenuProps
{
    
}

export default function HabitsAndRoutinesMenu({

}:HabitsAndRoutinesMenuProps){

    return (
        <View>
            <SafeAreaView>
                <Text>Consists of all ha bits and routines with settings to change them along with an option to initiate a routine</Text>
            </SafeAreaView>
        </View>
    )

}